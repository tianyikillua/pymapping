language: python

addons:
  apt:
    packages:
      - gmsh

python:
  - "3.7"

before_install:
  - pip3 install -U -r test_requirements.txt

install:
  - pip3 install -U .

script:
  - cd $TRAVIS_BUILD_DIR/test
  - pytest --cov pymapping

after_success:
  - codecov
